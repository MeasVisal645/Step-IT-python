# Regular function
def add(x, y):
    return x + y
print(add(5, 3)) # 8

# Lambda equivalent
add_lambda = lambda x, y: x + y
print(add_lambda(5, 3)) # 8

minus_lamda = lambda x, y: x - y
print(minus_lamda(5, 3)) # 2